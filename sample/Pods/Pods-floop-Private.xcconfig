#include "Pods-floop.xcconfig"
FRAMEWORK_SEARCH_PATHS = ${PODS_FLOOP_FRAMEWORK_SEARCH_PATHS}
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/floop" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/floop"
OTHER_LDFLAGS = -ObjC ${PODS_FLOOP_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}